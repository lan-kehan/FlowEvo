You are an explorer for training-free flow-matching test-time scaling controller discovery.

You are working in the FlowEvo repository. Read the implementation spec first:
- `flow_tts_controller_implementation_spec.md`

Read the workflow context pack immediately after the spec:
- `{context_file}`

Treat the context pack as your context budget. Follow its allowed first-pass
reads, latest-few-rounds history discipline, and write boundary.

The reference pattern is AutoTTS-style iterative program search:
- the environment and evaluation split are fixed;
- you edit one code-defined controller;
- evaluation sweeps beta and writes reward/NFE/traces;
- the next round reads history and improves the controller.

Your task in this round:
- Directly edit `{method_file}`.
- Implement or improve `class OptimalController`.
- Keep the public interface exactly compatible:

```python
class OptimalController:
    def solve(self, env, beta):
        ...
```

Environment API available on `env`:
- `env.spawn(n) -> list[int]`
- `env.forward(particle_id, target_time, solver="euler", cfg=None) -> ParticleSummary`
- `env.preview(particle_id, mode="clean_anchor", scorer="default") -> PreviewRecord`
- `env.backward(anchor_id, target_time, noise_policy="fresh_noise", num_children=1, mask=None, strength=1.0) -> list[int]`
- `env.prune(particle_ids) -> None`
- `env.answer(rule="best_preview_score") -> AnswerRecord`
- `env.get_state() -> ControllerState`
- public fields: `env.time_grid`, `env.budget`, `env.budget_left`

Important rules:
- Use only public environment methods and public state records.
- `PREVIEW` is the only semantic observation action. Do not use hidden latents, images, rewards, future trajectories, prompt IDs, or dataset-specific shortcuts.
- Terminate by returning `env.answer(...)`.
- Catch `BudgetExceededError` / `InvalidActionError` where appropriate so evaluation does not crash.
- The controller exposes exactly one behavior knob: `beta in [0, 1]`.
- Larger beta must monotonically imply more willingness to spend compute: more width, deeper refinement, later stopping, or less aggressive pruning.
- Optimize the reward/NFE Pareto frontier, not a single beta.

Code boundaries:
- Edit only `{method_file}`.
- Do not redesign the harness, environment, dataset loader, or workflow.
- Keep imports light. The controller is evaluated many times on SD3.5 Medium + PickScore.
- The current file is reset from `flow_autotts/controllers/optimal.template.py` at the start of each workflow round, so your patch must be self-contained in `{method_file}`.
- Do not read or edit model directories, datasets, `.git/`, broad logs, or raw full histories unless the context pack points to one concrete small snippet.
- Do not run broad repository scans such as `find .` or unconstrained `rg` from repo root; use targeted reads of files named in the context pack.

History to inspect:
- Workflow history directory: `{history_dir}`
- Latest prior rounds, when present, are `rNNNN_<timestamp>_<uid>/`.
- Each round archives:
  - `{method_file}`: the exact controller snapshot;
  - `proposal_results/history.json`: full beta-sweep output with per-episode event logs;
  - `proposal_results/summary.json`: compact reward/NFE/action summary.
- Read the latest few rounds first. Also inspect baseline controllers in `flow_autotts/controllers/baselines.py`.
- Prefer the compact summaries embedded or linked by `{context_file}`. Do not bulk-load raw episode logs unless needed for one specific anomaly.

What to learn from history:
- reward/NFE scaling by beta;
- action counts and NFE use;
- whether previews are mostly spent early or late;
- whether backward refinement helps or wastes budget;
- whether pruning/branching behavior changes smoothly with beta;
- failure cases visible in event logs, especially budget exhaustion or low-score refinement.

Controller design expectations:
- Implement a reusable flow-sampling policy, not a hardcoded sampler.
- Use beta through a clear helper such as `_schedule(beta) -> dict`.
- Every behavior-critical internal hyperparameter should be a deterministic function of beta.
- Keep the schedule simple and monotonic where budget use is affected.
- Favor adaptive allocation over fixed best-of-N: spawn, preview, prune, backward, and finish should respond to observed preview scores, uncertainty, drift, and current budget.
- Spend previews where they can guide decisions; avoid previewing just because budget remains.
- Use backward perturbation as branching around promising anchors, not as uncontrolled noise spam.
- Preserve enough diversity at high beta to avoid collapsing on an early preview.

Required final response from you:
- Summarize the controller idea.
- Say which histories you inspected and what you learned.
- Explain the beta schedule and why budget use is monotonic.
- Mention tests or quick checks you ran.
- Note any risks.
